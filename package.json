{
  "name": "reunionou-webapp",
  "scripts": {
    "eslint": "npx eslint . --ext .js --fix",
    "stylelint-css": "npx stylelint **/*.css --fix",
    "stylelint-scss": "npx stylelint **/*.scss --fix"
  },
  "devDependencies": {
    "@types/node": "^17.0.22",
    "eslint": "^8.11.0",
    "nodemon": "^2.0.15",
    "stylelint": "^14.0.1",
    "stylelint-config-standard": "^25.0.0",
    "stylelint-config-standard-scss": "^3.0.0"
  }
}
